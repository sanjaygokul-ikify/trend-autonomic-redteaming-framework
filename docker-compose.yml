version: '3.8'
services:
  redteam-core:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - REDTEAM_LOG_LEVEL=INFO
    command: uvicorn main:app --host 0.0.0.0 --port 8000